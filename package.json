{
  "name": "mcp-sqlite",
  "version": "1.0.1",
  "description": "SQLite CRUD MCP server (HTTP + MCP tools)",
  "main": "src/index.ts",
  "bin": {
    "mcp-sqlite": "dist/src/mcp-server.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "start": "npm run build && node dist/index.js",
    "dev": "ts-node src/index.ts",
    "build": "tsc",
    "mcp": "npm run build && node dist/mcp-server.js",
  "build:all": "tsc",
  "test:mcp": "npm run build && ts-node test/mcp-test.ts",
  "test:autos": "npm run build && ts-node test/mcp-create-autos.ts",
  "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.3",
    "express": "^4.18.2",
    "n8n-workflow": "^1.82.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.6"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "n8n": "^1.106.3",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.0"
  }
}
